{
  "name": "xrextgbot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && mkdir -p build && sass styles/styles.scss:build/styles/styles.css --no-source-map --style=compressed && cp -R assets js index.html login.html settings.html build/",
    "watch": "sass --watch styles/styles.scss:styles/styles.css --no-source-map --style=expanded"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redis": "^4.6.13"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
